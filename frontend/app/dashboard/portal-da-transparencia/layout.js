export const metadata = {
    title: 'SGM | Portal da Transparência',
    description: 'Portal da Transparência',
}

export default function PortalDaTransparenciaLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}